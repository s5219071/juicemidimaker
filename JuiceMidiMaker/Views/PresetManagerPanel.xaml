<UserControl x:Class="JuiceMidiMaker.Views.PresetManagerPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgSecondaryBrush}">
    <Border BorderBrush="#383838" BorderThickness="1,0,0,0" Padding="16,18,16,14">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,14">
                <TextBlock Text="PRESETS"
                           FontFamily="Consolas"
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="{StaticResource AccentBrush}" />
                <TextBlock Text="designed by Kino"
                           FontFamily="Consolas"
                           FontSize="10"
                           Foreground="{StaticResource TextMutedBrush}" />
            </StackPanel>

            <Border Grid.Row="1" Background="#191919" CornerRadius="3" Padding="8" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               VerticalAlignment="Center"
                               Text="{Binding PresetDirectory}"
                               ToolTip="{Binding PresetDirectory}"
                               TextTrimming="CharacterEllipsis"
                               Foreground="{StaticResource TextMutedBrush}"
                               FontSize="10" />
                    <Button Grid.Column="1"
                            Style="{StaticResource ActionButtonStyle}"
                            Padding="7,4"
                            FontSize="10"
                            Command="{Binding ChangePresetDirectoryCommand}"
                            Content="CHANGE" />
                    <Button Grid.Column="2"
                            Style="{StaticResource ActionButtonStyle}"
                            Padding="7,4"
                            FontSize="10"
                            Command="{Binding OpenPresetFolderCommand}"
                            Content="OPEN" />
                </Grid>
            </Border>

            <TextBox Grid.Row="2"
                     Style="{StaticResource DarkTextBoxStyle}"
                     ToolTip="Search by preset name or category"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

            <ComboBox Grid.Row="3"
                      Style="{StaticResource DarkComboBoxStyle}"
                      Margin="0,0,0,10"
                      ItemsSource="{Binding Categories}"
                      SelectedItem="{Binding SelectedCategory}" />

            <ListBox Grid.Row="4"
                     Margin="0,0,0,10"
                     Padding="0"
                     BorderThickness="0"
                     Background="#1A1A1A"
                     ItemsSource="{Binding FilteredPresets}"
                     SelectedItem="{Binding SelectedPreset}">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,0,0,1" />
                        <Setter Property="Background" Value="#202020" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BgHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#3A251A" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="10,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding PresetName}"
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{Binding Category}"
                                           Margin="0,2,0,0"
                                           FontSize="10"
                                           Foreground="{StaticResource AccentBrush}" />
                            </StackPanel>
                            <TextBlock Grid.Column="1"
                                       Text="{Binding UpdatedAt, StringFormat={}{0:yyyy-MM-dd}}"
                                       FontSize="10"
                                       Foreground="{StaticResource TextMutedBrush}" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Grid Grid.Row="5" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        Style="{StaticResource ActionButtonStyle}"
                        Command="{Binding LoadPresetCommand}"
                        IsEnabled="{Binding CanUseSelectedPreset}"
                        Content="LOAD" />
                <Button Grid.Column="1"
                        Style="{StaticResource ActionButtonStyle}"
                        Command="{Binding DeletePresetCommand}"
                        IsEnabled="{Binding CanUseSelectedPreset}"
                        Content="DELETE" />
            </Grid>

            <Expander Grid.Row="6"
                      Header="SAVE CURRENT PRESET"
                      Foreground="{StaticResource TextPrimaryBrush}"
                      IsExpanded="True">
                <StackPanel Margin="0,10,0,0">
                    <TextBlock Text="Preset name" FontSize="10" Foreground="{StaticResource TextMutedBrush}" />
                    <TextBox Style="{StaticResource DarkTextBoxStyle}"
                             Text="{Binding PresetSaveName, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Text="Category" FontSize="10" Foreground="{StaticResource TextMutedBrush}" />
                    <TextBox Style="{StaticResource DarkTextBoxStyle}"
                             Text="{Binding PresetCategory, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Text="Description (optional)" FontSize="10" Foreground="{StaticResource TextMutedBrush}" />
                    <TextBox Style="{StaticResource DarkTextBoxStyle}"
                             Height="48"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             Text="{Binding PresetDescription, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Style="{StaticResource ActionButtonStyle}"
                            Background="{StaticResource AccentBrush}"
                            BorderBrush="{StaticResource AccentBrush}"
                            Command="{Binding SavePresetCommand}"
                            Content="SAVE PRESET" />
                </StackPanel>
            </Expander>
        </Grid>
    </Border>
</UserControl>
